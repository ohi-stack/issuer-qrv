{
  "name": "ohi-stack/one-companion-plugin",
  "description": "Private WordPress companion plugin foundation for One Companion operations.",
  "type": "wordpress-plugin",
  "license": "proprietary",
  "autoload": {
    "classmap": [
      "includes/"
    ]
  },
  "require": {
    "php": ">=8.1"
  },
  "require-dev": {},
  "scripts": {
    "lint:php": "find . -path './vendor' -prune -o -name '*.php' -print0 | xargs -0 -n1 php -l"
  },
  "config": {
    "sort-packages": true
  }
}
{"name":"ohi-stack/one-companion-plugin","description":"WordPress companion plugin for ONE care operations.","type":"wordpress-plugin","license":"GPL-2.0-or-later","require":{"php":">=8.1"},"autoload":{"classmap":["includes/","database/","admin/","public/","api/"]},"scripts":{"lint":"php -l one-companion-plugin.php && find includes admin public api database -name '*.php' -print0 | xargs -0 -n1 php -l"}}
