import { Input } from '../shared/ui';

export function CertificateForm({ form, setForm }: { form: Record<string,string>; setForm: (v: Record<string,string>) => void }) {
  const set = (k: string, v: string) => setForm({ ...form, [k]: v });
  return <div className="grid"><label>certificateTitle<Input aria-label="certificateTitle" value={form.certificateTitle||''} onChange={(e)=>set('certificateTitle', e.target.value)} /></label><label>certificateType<Input aria-label="certificateType" value={form.certificateType||''} onChange={(e)=>set('certificateType', e.target.value)} /></label><label>credentialCategory<Input aria-label="credentialCategory" value={form.credentialCategory||''} onChange={(e)=>set('credentialCategory', e.target.value)} /></label><label>description<Input aria-label="description" value={form.description||''} onChange={(e)=>set('description', e.target.value)} /></label><label>issueDate<Input type="datetime-local" aria-label="issueDate" value={form.issueDate||''} onChange={(e)=>set('issueDate', e.target.value)} /></label><label>expirationDate<Input type="datetime-local" aria-label="expirationDate" value={form.expirationDate||''} onChange={(e)=>set('expirationDate', e.target.value)} /></label><label>internalCertificateId<Input aria-label="internalCertificateId" value={form.internalCertificateId||''} onChange={(e)=>set('internalCertificateId', e.target.value)} /></label><label>tags<Input aria-label="tags" value={form.tags||''} onChange={(e)=>set('tags', e.target.value)} /></label></div>;
}
