# API

REST endpoints are intentionally deferred. Every endpoint must declare required capabilities, validate nonces or application credentials, sanitize input, and record audit events for sensitive actions.
